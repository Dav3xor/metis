label         : /[a-zA-Z_][a-zA-Z0-9_]*/ ;

string        : /"(\\.|[^"])*"/ ;


unsigned      : /[0-9]+/ ;

integer       : /[-][0-9]+/ 
              | /[0-9]+/ ;

float         : /[-][0-9]+ /
              | /[0-9]+ /
              | /[-][0-9]+[.][0-9]* / 
              | /[0-9]+[.][0-9]* /;


vector        : '|' <float>+ '|' ;

matrix        : <vector> <vector>+ ;

term          : <factor> (('*' | '/' | '%' | "dot" | "cross") <factor>)* ;

lexp          : <term> (('+' | '-') <term>)* ;

type          : ("string" | "unsigned" | "integer" | "float" | "label" | "vector" | "matrix") ;

typeident     : <type> <label> ;

args          : <typeident>? (',' <typeident>)* ;

stmt          : "if" <exp> <stmt>* "end"
              | "while" <exp> <stmt>* "end"
              | "<-" <lexp> 
              | "<-|" <label>;

function      : "def" <label> <args> ("<-" <type>) ':' <stmt>* "end" ;

factor        : '(' <lexp> ')'
              | <string>
              | <unsigned>
              | <integer>
              | <float>
              | <label>
              | <vector>
              | <matrix> ;

metis         : /^/ <function> /$/ ;



